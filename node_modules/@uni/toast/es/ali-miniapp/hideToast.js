import { normalize } from "../utils/index";
import { isDingdingMiniapp } from "../_utils/miniappEnvApp";
var hide = normalize(function (options) {
  var hideToast = isDingdingMiniapp ? dd.hideToast : my.hideToast;
  hideToast(options);
});
export default hide;